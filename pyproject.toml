[build-system]
# requires = ["setuptools>=41.0", "wheel", "pccm>=0.4.16", "cumm>=0.9.1", "nvidia-arch>=7.0.0"]  # for cuda >= 12.6
requires = ["setuptools>=41.0", "wheel", "pccm>=0.4.16", "cumm>=0.7.14,<0.8.0", "nvidia-arch>=7.0.0"]  # for cuda < 12.6
build-backend = "setuptools.build_meta"
